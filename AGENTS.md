# Repository Guidelines

## プロジェクト構成とモジュール整理
- `src/core/` はドメイン層。`task.ts` / `type.ts` で値オブジェクトとサービスを定義し、`workflows/` がユースケースを組み立て、`repository/` が永続化ポートを公開します。
- IO・ログ・Temporal 変換はドメイン外に追い出し、時間処理は `src/util/date.ts` を経由して再利用します。
- `docs/` にはユビキタス言語 (`objective.md`, `design.md`) と意思決定履歴がまとまっているため、集約や境界づけられたコンテキストを変更する前に必ず参照します。
- 近々のリファクタや検討事項は `todo.md` に記し、進行中の PR と整合させてください。

## ビルド・テスト・開発コマンド
- `pnpm install` — ロックファイルを尊重して依存を復元します。
- `pnpm exec biome check .` — フォーマットとリンタ、インポート整列を一度に検証します。
- `pnpm exec biome format src docs` — レビュー前に整形を適用します。
- `pnpm run test` — 現状プレースホルダです。Vitest や Jest を導入したら script を追加し、ここに記載を残します。

## コーディングスタイルと命名
- 機能型コアを維持し、純粋関数・明示的な入出力・イミュータブルなデータ操作（スプレッドでの更新）を徹底します。
- ドメインエラーは `neverthrow` の `Result` で表現し、`createTask` や `updateStatus` のように不変条件が伝わる命名を選びます。
- インデントはタブ（Markdown は 2 スペース）。ダブルクォートと 100 桁以内の行長は Biome のフォーマッタで統一します。
- モジュール名は責務ベース (`task-repository.ts`, `schedule-workflow.ts`) とし、ドメイン用語と揃えます。

## 機能型 DDD プラクティス
- 集約は TypeScript の型＋振る舞い関数で表現し、永続化固有のフィールドは漏らしません。
- ワークフローはドメイン関数を合成し、リポジトリやインターフェースへ写像する役割に専念させ、インフラは差し替え可能なポートとして扱います。
- ビジネスルールはガード／バリデータとして関数化し `Result` を返却、複数ワークフローで再利用して不変条件を一元化します。
- 新機能はユビキタス言語から検討し、値オブジェクトを先に整備してからリポジトリや UI に展開します。

## テストガイドライン
- 純粋関数のユニットテストは高速かつ決定的にし、実装と同階層 (`task.test.ts`) に配置します。
- ワークフローは `neverthrow` のパターンマッチを使って成功／失敗の双方を検証します。
- 新しいリポジトリアダプタを追加するときは契約テストを用意し、PR でカバレッジの期待値を共有してください。

## コミットと PR 方針
- コミットは `feat:` `refactor:` `docs:` など履歴に沿ったプレフィクスを用い、本文でドメインへの影響や守った不変条件を説明します。
- 1 コミットにつき 1 集約または 1 ワークフローを原則とし、横断的なリファクタは理由とともに独立させます。
- PR ではユースケースの背景、実施したテスト、関連するドキュメント／Issue を明記し、UI 変更時のみスクリーンショットを添付します。
