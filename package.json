{
  "name": "taskell",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp fmt && vp run @taskell/core#build && vp lint && vp run -F '@taskell/*' test && vp run -F '@taskell/*' build",
    "build:sea": "vp run @taskell/cli#build:sea",
    "dev": "vp run @taskell/core#dev",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@typescript/native-preview": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=25.9.0"
  },
  "packageManager": "pnpm@10.33.0"
}
